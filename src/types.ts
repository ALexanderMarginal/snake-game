export enum Colors {
  Background = '#333',
  Border = '#000',
  Line = '#444',
  Snake = '#0f0',
  Apple = '#f00',
}

export enum Controls {
  ArrowUp = 'ArrowUp',
  ArrowDown = 'ArrowDown',
  ArrowLeft = 'ArrowLeft',
  ArrowRight = 'ArrowRight',
}

export type Coordinate = {
  x: number;
  y: number;
};
